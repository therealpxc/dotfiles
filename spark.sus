# spark is a system we can use to actually install these dotfiles -- selectively, if need be
card all {
  into ~
  spark card unicode
  spark card git
}

# compose key & other unicode delights
card unicode {
  .XCompose
}

# git stuff!
card git {
  .gitconfig
}
