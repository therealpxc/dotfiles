# note: since this file is in my omf config, it will assume that the usual
# omf plugins will be installed

# use balias extension to quickly alias fasd's 'z' autojump to traditional 'j'
alias j z				# aka jay-z
#alias grep ag   # I always forget to type the right thing, but ag is faster and mostly compatible
alias grep sift  # let's give sift a try over ag
alias ag sift
alias ls "ls --color=auto"
alias kquitapp kquitapp5

